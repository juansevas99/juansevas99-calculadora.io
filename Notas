1. You can avoid refresh the page if instead using submit type input you use a button or button type input
2. Can we add new values when the app is running to a variable if this varibles is out of any loop or functions